# Backend

Place the real API / simulation / data-processing source here.

Suggested areas:

- `api/` — endpoints
- `services/` — simulation, alerts, forecasting, AI/prediction
- `models/` — data models

Never commit secret API keys or passwords.
